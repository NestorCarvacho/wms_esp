import { Toaster } from '@/components/ui/shadcn/sonner';

/** Host global de notificaciones (Sonner / shadcn). */
const NotificationContainer = () => <Toaster />;

export default NotificationContainer;
